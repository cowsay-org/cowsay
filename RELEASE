Steps to be done for release:

0) Change version number in:
  a) cowsay file
  b) cowsay.1
1) Change date in cowsay.1
2) Git label the release
3) Create an X.Y-fixes branch, if this is a non-patch release
4) Push the release to GitHub
  a) TODO: Figure out how to do this. :)
